<!DOCTYPE html>
<html>
  <head>
    <title>Bootstrap 101 Template</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="stylesheets/bootstrap.min.css" rel="stylesheet" media="screen">
  </head>
  <body>
    <script src="http://code.jquery.com/jquery.js"></script>
    <script src="javascripts/bootstrap.min.js"></script>
    <div class="container">
      <div class="page-header"><h1>Pintetsy</h1></div>
      <% @listings.each do |listing| %>
        <div class="row listing">
          <div class="span4">
            <img src=<%= listing.item_image %> class="img-circle">
          </div>
          <div class="span8">
            <h3><a href=<%= listing.url %> ><%= listing.title %></a></h3>
            <ul>
              <li><strong>Price:</strong> $<%= listing.price %></li>
              <li><strong>Average Rating:</strong> <%= listing.feedback["score"] %>%</li>
              <li><strong>Total Sales:</strong> <%= listing.feedback["count"] %></li>
              <li><strong>Favorites:</strong> <%= listing.num_favorers %></li>
              <li><strong>Shop Owner:</strong> <%= listing.shop_owner %></li>
            </ul>
          </div>
        </div>
      <% end %>
    </div>
  </body>
</html>
